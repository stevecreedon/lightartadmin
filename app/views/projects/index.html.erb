<table>
  <tr>
	<th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
	<td><%= project.name %></td>
    <td>
    <%= link_to image_tag('document.png', :title => 'see this'), project %>
    <%= link_to image_tag('document_edit.png', :title => 'edit this'), edit_project_path(project) %>
    <%= link_to image_tag('document_delete.png', :title => 'delete this'), project, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to image_tag('document_add.png', :title => 'create a new one'), new_project_path %>
