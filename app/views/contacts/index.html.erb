

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.name %></td>
    <td>
    <%= link_to image_tag('document.png', :title => 'see this'), contact %>
    <%= link_to image_tag('document_edit.png', :title => 'edit this'), edit_contact_path(contact) %>
    <%= link_to image_tag('document_delete.png', :title => 'delete this'), contact, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to image_tag('document_add.png', :title => 'create a new one'), new_contact_path %>
