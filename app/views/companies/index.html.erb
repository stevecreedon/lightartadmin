

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
    <td><%= company.name %></td>
    <td>
    	<%= link_to image_tag('document.png', :title => 'see this'), company %>
		<%= link_to image_tag('document_edit.png', :title => 'edit this'), edit_company_path(company) %>
		<%= link_to image_tag('document_delete.png', :title => 'delete this'), company, :confirm => 'Are you sure?', :method => :delete %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to image_tag('document_add.png', :title => 'create a new one'), new_company_path %>
