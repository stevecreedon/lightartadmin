

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cost_centres.each do |cost_centre| %>
  <tr>
    <td><%= cost_centre.name %></td>
    <td>
    <%= link_to image_tag('document.png', :title => 'see this'), cost_centre %>
    <%= link_to image_tag('document_edit.png', :title => 'edit this'), edit_cost_centre_path(cost_centre) %>
    <%= link_to image_tag('document_delete.png', :title => 'delete this'), cost_centre, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to image_tag('document_add.png', :title => 'create a new one'), new_cost_centre_path %>
